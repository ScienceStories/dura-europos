import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  iframe: {
    border: 'none',
    height: '65vh',
    width: '100%',
  },
}));

export default useStyles;

import { Color } from './constants';

const theme = {
  palette: {
    primary: {
      main: Color.Blue,
    },
    secondary: {
      main: Color.Orange,
    },
  },
  typography: {
    button: {
      fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif',
    },
    fontFamily: '"YaleNew", "Arial", sans-serif',
    overline: {
      fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif',
    }
  },
};

export default theme;
